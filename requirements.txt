# ──────────────────────────────────────────────────────────────
# requirements.txt — NeuroScript AI
# Tested: Python 3.13 · Streamlit Cloud (uv + pip)
# ──────────────────────────────────────────────────────────────

# ── Streamlit & UI ─────────────────────────────────────────────
streamlit>=1.31.0
streamlit-option-menu>=0.3.0
streamlit-extras>=0.3.0
streamlit-drawable-canvas>=0.9.3

# ── Data & Plotting ────────────────────────────────────────────
pandas>=2.2.0
numpy>=2.0.0
plotly>=5.19.0
plotly-express>=0.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
bokeh>=3.4.0

# ── Image & Vision ─────────────────────────────────────────────
pillow>=10.2.0
# FIX: opencv-python requires libGL.so.1 (OpenGL) which is NOT available
# on Streamlit Cloud servers. Use headless variants — identical API, no GUI deps.
opencv-python-headless>=4.9.0
opencv-contrib-python-headless>=4.9.0
scikit-image>=0.22.0

# ── ML / Deep Learning ─────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0
scikit-learn>=1.4.0
scipy>=1.12.0

# ── NLP / Transformers ─────────────────────────────────────────
transformers>=4.37.0
datasets>=2.17.0
sentencepiece>=0.2.0
accelerate>=0.27.0
einops>=0.7.0

# ── Signal Processing ──────────────────────────────────────────
# FIX: 'pywt' is NOT a valid pip package name on Python 3.13.
# The correct PyPI name is 'PyWavelets' (installs as the pywt module).
PyWavelets>=1.6.0
